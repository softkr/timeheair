name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
          - platform: "macos-latest"
            args: "--target x86_64-apple-darwin"
          - platform: "ubuntu-22.04"
            args: ""
          - platform: "windows-latest"
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || (matrix.platform == 'windows-latest' && 'x86_64-pc-windows-msvc') || 'x86_64-unknown-linux-gnu' }}

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        run: yarn install

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "타임헤어 v__VERSION__"
          releaseBody: |
            ## 타임헤어 v__VERSION__ 릴리스

            ### 🚀 주요 변경사항 (v0.1.5)
            - **Rust 백엔드 통합**: Go 백엔드를 Rust로 완전히 재구현
            - **성능 향상**: HTTP → IPC 통신으로 응답 속도 개선
            - **단일 바이너리**: 별도 서버 프로세스 불필요
            - **빠른 시작**: 앱 실행 즉시 사용 가능

            ### 설치 방법
            - **Windows**: `.msi` 파일 다운로드 후 실행
            - **macOS**: `.dmg` 파일 다운로드 후 실행 (Apple Silicon 및 Intel 지원)
            - **Linux**: `.AppImage` 또는 `.deb` 파일 다운로드

            ### 주요 기능
            - 고객 관리 (회원 정보, 스탬프 적립)
            - 예약 관리 (실시간 좌석 현황)
            - 직원 관리
            - 매출 대시보드 및 통계
            - 자동 업데이트

            ### 기술 스택
            - Tauri 2.0 + Rust (백엔드)
            - Next.js + React + Ant Design (프론트엔드)
            - SQLite (데이터베이스)
            - JWT 인증

            ### 자동 업데이트
            - 앱 시작 시 자동으로 업데이트 확인
            - 새 버전 발견 시 자동 다운로드 및 설치 가능
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
